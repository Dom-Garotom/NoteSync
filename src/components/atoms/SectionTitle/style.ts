import { StyleSheet } from "react-native"

const StyledSectionTitle = StyleSheet.create({
    title: {
        width: '100%',
        fontSize: 28,
        fontWeight: '600',
        color: '#000',
    },
})

export {
    StyledSectionTitle
}
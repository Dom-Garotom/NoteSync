import { StyleSheet } from 'react-native';

const StyledPageTemplate = StyleSheet.create({
	page: {
		flex: 1,
		padding: 25,
		gap: 33,
		alignItems: 'center',
	},
});

export { StyledPageTemplate };

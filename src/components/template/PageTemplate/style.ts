import { StyleSheet } from "react-native"

const StyledPageTemplate = StyleSheet.create({
    page: {
        flex: 1,
        paddingVertical: 30,
        paddingHorizontal: 20,
        backgroundColor: '#fff',
        alignItems: 'center',
    },
})

export {
    StyledPageTemplate
}
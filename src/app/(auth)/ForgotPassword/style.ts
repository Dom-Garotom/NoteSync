import { StyleSheet } from 'react-native';

const StyledForgotPassword = StyleSheet.create({
	arrowLeft: {
		position: 'absolute',
		left: 15,
		top: 30,
		padding: 10,
	},
});

export { StyledForgotPassword };

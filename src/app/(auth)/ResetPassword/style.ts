import { StyleSheet } from 'react-native';

const StyledResetPassword = StyleSheet.create({
	arrowLeft: {
		position: 'absolute',
		left: 15,
		top: 30,
		padding: 10,
	},

	bottomText: {
		width: 220,
		textAlign: 'center',
	},
});

export { StyledResetPassword };
